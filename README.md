# Terraform Lab

This is a local Terraform lab running in WSL Ubuntu with Docker Desktop integration.

## What it does

Terraform creates a local Docker container running Nginx.

After applying, open:

http://localhost:8080

## Commands

Initialize Terraform:

```bash
terraform init